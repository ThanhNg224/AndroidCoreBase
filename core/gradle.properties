# Source of truth for :core's published version when building locally (no JitPack VERSION
# env var present). Bump this together with cutting a matching git tag for a real release —
# e.g. set VERSION_NAME=v1.0.1 here and `git tag v1.0.1` in the same change.
VERSION_NAME=v2.0.0
